services:
  anvil:
    image: ghcr.io/foundry-rs/foundry:latest
    entrypoint: ["anvil", "--host", "0.0.0.0", "--port", "8545", "--block-time", "1", "--silent"]
    ports:
      - "8545:8545"
    healthcheck:
      test: ["CMD", "cast", "bn", "--rpc-url", "http://localhost:8545"]
      interval: 3s
      timeout: 3s
      retries: 10

  deploy:
    image: ghcr.io/foundry-rs/foundry:latest
    volumes:
      - ./contracts:/app/contracts
      - ./scripts:/app/scripts
      - ./contracts/.solc-cache/solc-0.8.24:/home/foundry/.svm/0.8.24/solc-0.8.24:ro
    entrypoint: ["sh", "/app/scripts/deploy-local.sh"]
    environment:
      ANVIL_URL: http://anvil:8545
      DEPLOYER_PRIVATE_KEY: "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
      OUTPUT_FILE: /app/contracts/deployments/local.json
    depends_on:
      anvil:
        condition: service_healthy

  db:
    image: timescale/timescaledb:2.17.2-pg16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: agentproof
      POSTGRES_USER: agentproof
      POSTGRES_PASSWORD: localdev
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./src/agentproof/pipeline/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./src/agentproof/pipeline/schema_mcp.sql:/docker-entrypoint-initdb.d/02-schema_mcp.sql
      - ./src/agentproof/pipeline/schema_alerts.sql:/docker-entrypoint-initdb.d/03-schema_alerts.sql
      - ./src/agentproof/pipeline/schema_benchmarks.sql:/docker-entrypoint-initdb.d/04-schema_benchmarks.sql
      - ./src/agentproof/pipeline/schema_benchmark_config.sql:/docker-entrypoint-initdb.d/05-schema_benchmark_config.sql
      - ./src/agentproof/pipeline/schema_routing.sql:/docker-entrypoint-initdb.d/06-schema_routing.sql
      - ./src/agentproof/pipeline/schema_cache_tokens.sql:/docker-entrypoint-initdb.d/07-schema_cache_tokens.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agentproof"]
      interval: 5s
      timeout: 5s
      retries: 5

  litellm:
    profiles: ["proxy"]
    image: ghcr.io/berriai/litellm:main-stable
    ports:
      - "4000:4000"
    volumes:
      - ./litellm-config.example.yaml:/app/config.yaml
      - ./src/agentproof:/app/agentproof
      - ./pyproject.toml:/app/pyproject.toml
    entrypoint: ["sh", "-c", "pip install --no-cache-dir /app && litellm --config /app/config.yaml --port 4000"]
    environment:
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      AGENTPROOF_DATABASE_URL: postgresql+asyncpg://agentproof:localdev@db:5432/agentproof
    depends_on:
      db:
        condition: service_healthy

  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - "8100:8100"
    volumes:
      - ./src:/app/src
      - ./contracts/deployments:/app/contracts/deployments:ro
    environment:
      AGENTPROOF_DATABASE_URL: postgresql+asyncpg://agentproof:localdev@db:5432/agentproof
      AGENTPROOF_UPSTREAM_URL: ${AGENTPROOF_UPSTREAM_URL:-http://litellm:4000}
      AGENTPROOF_ANTHROPIC_UPSTREAM_URL: https://api.anthropic.com
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      AGENTPROOF_ENV: development
      AGENTPROOF_ROUTING_ENABLED: "true"
      AGENTPROOF_BENCHMARK_ENABLED: "true"
      AGENTPROOF_ATTESTATION_PROVIDER: evm
      AGENTPROOF_ATTESTATION_RPC_URL: http://anvil:8545
      AGENTPROOF_ATTESTATION_PRIVATE_KEY: "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
      AGENTPROOF_ATTESTATION_DEPLOYMENTS_PATH: /app/contracts/deployments/local.json
    depends_on:
      db:
        condition: service_healthy
    command: ["uvicorn", "agentproof.api.app:app", "--host", "0.0.0.0", "--port", "8100", "--reload"]

  dashboard:
    image: node:20-slim
    working_dir: /app
    ports:
      - "8081:5173"
    environment:
      CI: "true"
      API_URL: "http://api:8100"
    volumes:
      - ./dashboard:/app
    command: ["sh", "-c", "corepack enable && pnpm install --frozen-lockfile && pnpm dev --host 0.0.0.0"]
    depends_on:
      api:
        condition: service_started

volumes:
  pgdata:
